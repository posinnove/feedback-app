# Copy this file to `.env` and fill in values for your environment
# DATABASE_URL takes precedence over the other variables if provided.
# For local development fill in individual settings instead.

# DATABASE_URL=postgres://user:password@localhost:5432/feedback_app

DB_HOST=localhost
DB_PORT=5432
DB_NAME=feedback_app
DB_USER=postgres
DB_PASSWORD=postgres

PORT=3000
NODE_ENV=development
DB_LOGGING=false
